[plugin]

name="Rollback"
description="Deterministic GGPO-style rollback netcode core for Godot 4: fixed-tick simulation, per-tick snapshots + content hashing, sync-test determinism harness, and a WebRTC/WebSocket transport with input-delay, prediction, checksums, and host resync."
author="Animo Games"
version="0.3"
script="rollback_plugin.gd"
